# LOCED — Loc Education Website

Next.js + TypeScript + Tailwind CSS + Supabase starter for an instructional loc-care website.

## Run
npm install
npm run dev

## Supabase
Copy `.env.example` to `.env.local`, add the Supabase URL and publishable key, then run `supabase/schema.sql` in the Supabase SQL editor.
